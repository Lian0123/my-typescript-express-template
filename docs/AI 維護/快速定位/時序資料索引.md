<!-- AI-DOC: owner=maintainers; verified=2026-07-10; sources=src/timeseries,src/migrations/1783641600000-time-series-samples.ts,src/migrations/1783641600001-time-series-rollups.ts,docker-compose.timeseries.yaml -->
# 時序資料索引

## 路徑與責任

| 層 | 路徑 | 責任 |
|---|---|---|
| HTTP | `src/timeseries/time-series.controller.ts` | 時序樣本寫入、查詢、彙總、downsample、retention |
| 業務 | `src/timeseries/time-series.service.ts` | 驗證範圍、轉派 repository、整理回應 |
| 資料 | `src/timeseries/repositories/time-series.repository.ts`、`src/timeseries/repositories/time-series-rollup.repository.ts` | raw sample 與 rollup 的新增、區間查詢、bucket 彙總、清理 |
| DB | `src/timeseries/entities/time-series-sample.entity.ts`、`src/timeseries/entities/time-series-rollup.entity.ts`、`src/migrations/1783641600000-time-series-samples.ts`、`src/migrations/1783641600001-time-series-rollups.ts` | `time_series_samples`、`time_series_rollups` 與索引 |
| 部署 | `docker-compose.timeseries.yaml` | 直接切換成 TimescaleDB 容器 |
| 輸入／輸出 | `dto/`、`ao/`、`bo/`、`po/` | request 驗證與 response 形狀 |
| 測試 | `src/test/timeseries/time-series.service.spec.ts` | service 與 contract 行為 |

## HTTP contract 快覽

- `POST /v1/time-series/samples`: 寫入單筆 sample。
- `POST /v1/time-series/samples/batch`: 寫入多筆 sample。
- `GET /v1/time-series/samples`: 依 `seriesKey`、時間區間、`metricName`、`limit`、`offset` 查詢。
- `GET /v1/time-series/summary`: 依 `bucket` 對同一查詢條件做彙總。
- `POST /v1/time-series/maintenance/downsample`: 把指定區間的 raw sample 寫入 rollup table。
- `POST /v1/time-series/maintenance/retention`: 依 cutoff 刪除 raw sample，並可選擇同步清理 rollup。

## 時序處理不變條件

- 任何查詢都必須帶 `seriesKey`、`startAt`、`endAt`。
- 時間區間採半開區間 `startAt <= recordedAt < endAt`，避免跨桶重疊。
- `bucket` 僅接受 `minute`、`hour`、`day`、`week`、`month`。
- repository 以 PostgreSQL 為基礎，migration 會在偵測到 TimescaleDB extension 時嘗試建立 hypertable，但不是硬性依賴。
- downsample 預設先保留 raw data；retention endpoint 是主動清理入口，避免把留存和壓縮混在同一個動作裡。

## 修改檢查

新增欄位或索引時，先改 entity + migration，再改 repository query，最後補 controller DTO、AO 與測試。
