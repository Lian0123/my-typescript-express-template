<!-- AI-DOC: owner=maintainers; verified=2026-07-10; sources=package.json -->
# 外部參考索引

目前未保存外部文件快照。需要框架、套件或標準資訊時，優先查對應版本的官方文件與 release notes；技術問題不要只依賴部落格或搜尋摘要。

| 主題 | 版本線索 | 查詢目的 |
|---|---|---|
| TypeScript | `package.json`、`tsconfig.json` | 編譯選項與 decorator 相容性 |
| Express / Inversify | `package.json` | middleware、DI、controller lifecycle |
| TypeORM | `package.json` | 0.3 migration、DataSource、repository API |
| RabbitMQ / amqplib | `package.json` | ack/nack、confirm channel、重試可靠性 |
| Jest / ts-jest | `package.json`、`jest.config.js` | TypeScript 測試與版本相容性 |
| Socket.IO | `package.json` | namespace、ack、adapter、重連 |

若必須保留專案專屬摘要，在本目錄新增一份檔案並包含：來源 URL、取用日期、適用版本、摘要、影響範圍；從本索引連結。不要複製整篇外部內容，避免版權、過時與 token 浪費。
