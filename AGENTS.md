# AI 協作規則

任何維護或開發任務都先讀取 `docs/AI 維護/入口.md`，再只讀取入口指向的最小必要文件。

- 以程式碼、設定與可執行結果為最終事實來源；文件衝突時先驗證，再同步文件。
- 修改程式時，依 `docs/AI 維護/文件生成索引.md` 判斷受影響文件。
- 不猜測環境值、不讀出或提交密鑰、不把未完成設計寫成既有能力。
- 完成前執行與變更風險相稱的驗證；至少執行 `npm run docs:verify`。
- 新增功能時建立或更新 `docs/AI 維護/快速定位/` 下的索引。
- 只記錄能幫助下一個任務的穩定知識；過程對話與暫時性細節不要寫入重點記憶。
